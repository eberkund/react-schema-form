{
  "name": "@react-schema-form/utils-validate",
  "version": "1.0.0",
  "description": "JSON Schema validation for React JSON Schema Form",
  "license": "MIT",
  "main": "build/index.js",
  "module": "lib/index.js",
  "variable": "ReactSchemaFormUtilsValidate",
  "scripts": {
    "start": "cross-env NODE_ENV=development babel src --out-dir lib --watch",
    "build:lib": "cross-env NODE_ENV=production babel src --out-dir lib",
    "build:umd": "neutrino build --use ../../.neutrinorc.js",
    "build": "npm run build:lib && npm run build:umd"
  },
  "dependencies": {
    "ajv": "^6.5.0",
    "lodash.topath": "^4.5.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "cross-env": "^5.1.6",
    "neutrino": "^8.2.3",
    "react": "^16.4.0"
  }
}
