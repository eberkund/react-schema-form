{
  "name": "@react-schema-form/utils",
  "version": "1.0.0",
  "description": "React Schema Form Utils",
  "license": "MIT",
  "main": "build/index.js",
  "module": "lib/index.js",
  "variable": "ReactSchemaFormUtils",
  "scripts": {
    "start": "cross-env NODE_ENV=development babel src --out-dir lib --watch",
    "build:lib": "cross-env NODE_ENV=production babel src --out-dir lib",
    "build:umd": "neutrino build --use ../../.neutrinorc.js",
    "build": "npm run build:lib && npm run build:umd"
  },
  "dependencies": {
    "@react-schema-form/utils-validate": "^1.0.0",
    "setimmediate": "^1.0.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "cross-env": "^5.1.6",
    "neutrino": "^8.2.3",
    "react": "^16.4.0"
  }
}
